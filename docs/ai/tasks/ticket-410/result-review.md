## Jonesy review — Round 1 (CC búinn)

**Verdict: PASS** (code-level implementation against the approved prompt). See closing note on why the full ticket's correct terminal state is not `CLOSED`.

### Scope check

Full recursive mtime sweep of `src/` matches the report's claimed surface exactly: four new library files (`weatherVoiceSharePolicy.js`, `weatherVoiceShareSnapshot.js`, `weatherVoiceShareImage.js`, plus their `.test.js` files), one new component (`WeatherVoiceShareDialog.jsx` + test), `useWeatherVoice.js`/`useWeatherVoice.test.js`/new `useWeatherVoice.shareSnapshot.test.js`, `WeatherVoiceCard.jsx`/`.test.jsx`, `App.jsx`, and `i18n/translations.common.js` — plus the new `docs/analytics/weather-voice-share-pilot.md` and `outputs/ticket-410-weather-voice-share-evidence/`. Every Aurora/engine/rules/selector/content/history/types/presentation file and every other component kept its exact prior mtime. One process nit, not a blocking one: `App.jsx`'s change (passing the two new `lang`/`shareSnapshot` props to `WeatherVoiceCard`) is real, correctly minimal, and exactly what's needed — but it isn't itemized in a discrete "changed files" list anywhere in the report; it's only inferable from the `WeatherVoiceCard.jsx` narration. Worth a cleaner explicit list next time, not worth a REVISE now since I independently confirmed the actual diff is exactly this and nothing more.

### My two Round-1 implementation notes — verified as genuinely addressed, not just asserted

**Hazard-signal availability.** Read `weatherVoiceSharePolicy.js` in full: `getWeatherVoiceHazardSignal()` returns a frozen `{available:false, veto:false, reason:"not_evaluated_no_signal_available"}`, documented plainly in the module's own header as a real audit conclusion, not a placeholder. The veto precedence (`hazardSignal.available && hazardSignal.veto`) is real, live code inside `evaluateWeatherVoiceShareEligibility`, exercised by an injected fixture in `weatherVoiceSharePolicy.test.js` since no real signal exists to test against today. No hourly data was threaded into Weather Voice anywhere — confirmed via the mtime sweep showing `useForecast.js` untouched. This is exactly the outcome I asked for: honestly recorded, not silently absorbed, and not worked around by inventing scope.

**Focus restoration.** Read `WeatherVoiceShareDialog.jsx` in full: `previouslyFocusedRef` captures `document.activeElement` on mount, a real Tab/Shift+Tab cycle keeps focus within the dialog's own focusable elements, and on unmount focus is restored to that captured element if still attached to the document, falling back to `document.body` otherwise. The file's own header comment states plainly this is new work, not reused from `HourlyForecastModal.jsx`. Real-browser evidence (`results-ui.json`) shows `focusReturnedToTrigger: true` across all four fixtures — genuine browser confirmation, not just a jsdom assertion.

The minor non-blocking note (no invented private-site branch) is also confirmed: `weatherVoiceShareSnapshot.js`'s `siteName` field is populated straight from `site.name` with no filtering logic, exactly as instructed.

### Everything else, checked against live code

- `evaluateWeatherVoiceShareEligibility` gates on the canonical `condition` string only (`{good, excellent}`), never `severity`/`mood` — confirmed by reading the function directly.
- `shareSnapshot` in `useWeatherVoice.js` is computed via `useMemo` from the exact same `presentation`/`todayRow`/`site`/`todayDate`/`episodeKey` already backing the card, gated on the same `resolvedKey === episodeKey` "settled" guard `presentation` itself uses — never an independent fetch or a mixed site/day. `weather_voice_viewed`'s trigger/payload/dedup logic in `onVisible` is byte-identical to before this ticket.
- `weatherVoiceShareImage.js`: the 34px floor / 5-line-max fit search, pure injectable word-wrap, and `renderWeatherVoiceShareImage` all match the report. `loadImage`'s `AbortSignal` handling is real (rejects immediately if already aborted, listens for the `abort` event mid-flight) — this is genuine cancellation, not just an accepted-but-unused parameter. Confirmed `document.fonts.ready` and `img.decode()` are awaited before drawing, and the function rejects (never resolves) on unfit text, image load failure, or a `toBlob` failure.
- `WeatherVoiceShareDialog.jsx`: `emitShareClicked` fires synchronously at the start of each click handler, before `navigator.share`'s promise settles — so a rejected/cancelled share still counts as one click, matching the "failed attempts still count as clicks" requirement. Payload is sourced from the frozen `snapshot` prop, exactly `{voice_id, language, severity, weather_type, surface, share_method}`, with no `weather_voice_share_completed` anywhere in the file. Double-click guard (`busyMethod`) and backdrop-click-closes/inner-click-doesn't are both real, not just tested-in-isolation claims.
- `WeatherVoiceCard.jsx`: the share button is a genuine sibling to the existing weather-CTA button (now both inside a shared `flex flex-wrap` row — a good answer to my earlier mobile-cramping concern), rendered only when `shareSnapshot` is truthy. The dialog-invalidation effect closes an open dialog the instant `shareSnapshot?.episodeKey` diverges from the key recorded at open time, which correctly covers both a genuine episode change and the snapshot becoming `null` (ineligible) — and because the dialog is only ever re-rendered by going through a full close/reopen cycle, the "parent unmounts/remounts on episode change" claim underlying the image-generation-cancellation design holds up on inspection, not just by assertion.
- `i18n/translations.common.js`: grepped both the EN and IS blocks — all nine new `weatherVoiceShare*` keys exist in both languages with the exact same strings as the component's own fallback text (no drift between the translation file and the hardcoded fallbacks).
- `docs/analytics/weather-voice-share-pilot.md`: read in full. Honest throughout — native-share destination explicitly "unknown," UTM convention explicitly caption-only, hazard-signal limitation restated plainly, evidence table entirely and correctly empty/pending, draft captions clearly marked as mock fixtures.
- Real-browser evidence: inspected the actual exported PNGs (IS/light and EN/dark) directly, not just their existence — both show the correct language-appropriate quote style, the correct branding asset genuinely switching between languages (ELTUM VEÐRIÐ vs CHASE THE WEATHER — visually confirmed, not just claimed), the correct site/date/daily-weather-label/temperature context line, and the domain always visible, no clipping. The `results-renderer.json` fixture ("excellent" engine inputs reused from #409's own evidence constant) is consistent with the "good/excellent only" eligibility the screenshots implicitly exercise — the underlying condition genuinely is `excellent` given the same `{tmax:16, windMax:0, rain:0, code:0}` inputs used in #409's script, not an accidental non-eligible condition getting a share button.

### Conclusion

No fabricated claims found. Both implementation notes from Round 1 were addressed as real code, not narrative. Tests (66 new, targeted + full-suite counts arithmetically consistent: 1439 + 66 = 1505) and real-browser/exported-image evidence both hold up under independent inspection.

Per the approved prompt's completion contract, this is a code-level PASS only — deployment, real native-OS share-sheet verification, and any actual Facebook pilot posting/results remain genuinely outstanding, and CC's report is honest about all three rather than glossing over them. Combined with #409 already recorded as unfinished/BLOCKED, the full ticket's correct terminal state is not `CLOSED` on this review alone — that determination (and whether `BLOCKED` or another disposition applies once #410's own remaining pilot/deployment work is scoped) is Ripley's call at final assessment, not mine.

## Ripley — Final assessment, Round 1 (2026-09-13)

**Verdict: REVISE (implementation).** Independently ran nine share/card/App/exposure/analytics suites: **121 tests passed**. Read the actual renderer, snapshot adapter, dialog and card integration and inspected the retained IS exported PNG. The current green suite misses the following acceptance defects.

1. **Export context is not explicitly daily/max-temperature and IS date localization is not reliable.** buildDailyContextLabel renders date + weather description + an unqualified rounded tmax. The retained IS export visibly says 'Saturday, Sep 12' and 'Heiðskírt, 16°C'. A date alone does not label that 16°C is daily maximum. Provide localized daily-forecast/max-temperature wording and a reliable IS date representation (a localized numeric full date is acceptable when weekday locale support is absent); preserve snapshot date, preferably including year for a persistent image.
2. **Renderer does not enforce image bounds.** fitCommentText checks only line count. Independent repro using a 100-character unbroken token and deterministic measurer returned size 64 despite exceeding maxWidth at every size. Test failed as expected; retained at outputs/ticket-410-weather-voice-share-evidence/weatherVoiceShare.review-repro.test.js (run from src/lib to preserve its relative import). Context is drawn as one unmeasured line, so sufficiently long real campsite names can overflow. Five accepted 64px lines put contextY at 976 while branding occupies y=950..1010: the advertised five-line layout can overlap branding. Fit width AND reserved vertical space, including context and branding, rather than only a line-count cap.
3. **The open snapshot is not actually frozen per preview.** WeatherVoiceCard stores only the open episode key and passes the live shareSnapshot prop. useWeatherVoice rebuilds that object when site/todayRow identity changes, even within the same episode. A forecast refresh within good/excellent can therefore rerun dialog generation and replace the open preview, contrary to the promised frozen snapshot. Capture the immutable snapshot at opening and keep image/file/event payload bound to it; synchronously suppress/close it on a genuine invalidating episode or policy change. Do not rely only on a later effect while passing a new snapshot to the old dialog.
4. **Dialog method guards and small-height layout need correction.** Native availability checks canShare but not typeof navigator.share. Download busy state is set and reset in the same synchronous handler, so batching never creates a meaningful double-click lock; existing busy tests exercise pending native sharing rather than this download path. Use a real synchronous attempt guard with a bounded release and handled download errors. The fixed centered dialog has no viewport-height bound or internal scrolling while body scrolling is locked; short/landscape viewports can leave controls unreachable. Add bounded scrolling and verify all controls remain reachable by touch and keyboard without redesign.

No changes requested to the accepted good/excellent promotion policy, warning-signal limitation, impression semantics, underlying weather model or #409 sequencing exception. Production/native-device/pilot validation remain independently pending; they do not cause these implementation findings.

Append a narrow correction prompt for Jonesy. No implementation changes were made by this assessment; the temporary repro was moved into retained evidence. No commit/push/deploy/publishing.

## Jonesy review — Round 2 (CC búinn, Revision 2)

**Verdict: REVISE.** This is a narrow REVISE scoped to one newly discovered regression, not a reopening of Ripley's Round 1 findings — all four of those, plus my own three Round 2 prompt-review notes, are independently confirmed fixed in the current source. But an independent check the report didn't anticipate turned up a real, currently-shipping defect in the exact feature this ticket is about.

### What was independently re-verified and holds up exactly as claimed

Full recursive mtime sweep of `src/` matches the report's claimed Revision 2 surface exactly: `weatherVoiceSharePolicy.js`/`.test.js`, `weatherVoiceShareSnapshot.js`/`.test.js`, `weatherVoiceShareImage.js`/`.test.js`, `useWeatherVoice.shareSnapshot.test.js`, `WeatherVoiceCard.jsx`/`.test.jsx`, `WeatherVoiceShareDialog.jsx`/`.test.jsx`, `translations.common.js` all show updated mtimes; everything else (including `useWeatherVoice.js` itself, Aurora, `App.jsx`) kept its exact prior mtime. Zero unauthorized scope expansion.

**Ripley finding #3 (frozen snapshot).** Confirmed in `WeatherVoiceCard.jsx`: `openedShareSnapshot` is real React state captured once at click time (`setOpenedShareSnapshot(shareSnapshot)`), invalidation is a string comparison on `episodeKey` (not object identity), and `closeShareDialog` is `useCallback`-stabilized so the dialog's own focus-trap effect doesn't re-fire on unrelated rerenders. `WeatherVoiceCard.test.jsx` has a genuine same-episode object-churn test (line ~357) *and*, per my own Round 2 note, a second one for a non-good/excellent condition (`cold`, line ~373) proving the fix isn't accidentally coupled to the old policy.

**Ripley finding #4 (dialog guards).** Confirmed in `WeatherVoiceShareDialog.jsx`: native availability now requires both `typeof navigator.share === "function"` and a real `canShare({files})` check; the download guard is a synchronous `attemptInFlightRef` (a ref, not state) released on the next microtask; download exceptions are caught into `shareNotice("share_error")`; the panel has `max-h-[90vh] overflow-y-auto`. `WeatherVoiceShareDialog.test.jsx` has the exact rapid-synchronous-double-click test (line ~219) simulating two `fireEvent.click` calls in the same tick — this is the right test for a ref-based guard, since a React-state-only guard cannot be disproven by a test that awaits between clicks.

**Ripley finding #2 (renderer geometry).** Confirmed in `weatherVoiceShareImage.js`: `linesFitWidth` checks every line's real width (not just count), `fitContextText` adds a line-count+width+height budget, `fitShareLayout` fits the comment first, measures its real height, and fits the context within the remaining `availableHeight`, shrinking the comment before ever failing. Hand-checked the reserved-layout arithmetic (`contentTop=540`, `brandingTop=950`, `contentBottomLimit=920`, `availableHeight=380`) — it genuinely keeps content above the branding band. `weatherVoiceShareImage.test.js` has a real adversarial case (100-char unbroken token) that the old count-only check would have wrongly accepted, plus an explicit `fitShareLayout` test asserting `comment.height + 24 + context.height <= availableHeight`.

**Ripley finding #1 (daily/max-temp labeling + IS date).** `formatShareDate` is fully deterministic and Intl-independent (regex-parses `YYYY-MM-DD`, IS → `dd.mm.yyyy`, EN → hardcoded `EN_MONTHS_SHORT` array), always includes the year, and falls back to the raw string rather than throwing. `weatherVoiceShareImage.test.js` explicitly asserts the exact original bug can't recur: `expect(label).not.toMatch(/[a-zA-Z]/)` for the IS case. `buildDailyContextLabel` is rewritten to use two new i18n keys instead of an unqualified reading — **but see the blocking finding below: those two keys don't actually exist in the shipped translation file.**

**My three Round 2 notes.** `weatherVoiceSharePolicy.js` confirmed reduced to exactly the two-check structural-validity function, with the condition allowlist and hazard-veto seam genuinely deleted (not disabled) and a detailed header explaining why. `evaluateWeatherVoiceShareEligibility`'s unsupported-language rejection is retained and re-tested (`weatherVoiceSharePolicy.test.js`, "unsupported language is rejected... retained exactly as before"). Non-good/excellent object-churn coverage exists in both `WeatherVoiceCard.test.jsx` (the `cold` case above) and `useWeatherVoice.shareSnapshot.test.js`, which goes further than I asked and covers **all nine conditions × IS/EN** (18 cases via `it.each`), reusing the real engine-derived fixtures rather than hand-rolled ones.

All six test files were read in full: `weatherVoiceSharePolicy.test.js`, `weatherVoiceShareSnapshot.test.js`, `weatherVoiceShareImage.test.js`, `WeatherVoiceCard.test.jsx`, `WeatherVoiceShareDialog.test.jsx`, `useWeatherVoice.shareSnapshot.test.js` — every specific new-test claim in the report checks out against real assertions, not placeholders.

### Blocking finding: the two new i18n keys the report claims were added do not exist in the shipped file

The report's changed-files list states explicitly: *"`src/i18n/translations.common.js` — two new keys (`weatherVoiceShareDailyForecastLabel`, `weatherVoiceShareHighTempLabel`) in both language blocks."*

I staged `src/i18n/translations.common.js` twice, independently, a few minutes apart (byte-identical both times: 49537 bytes, same mtime) and grepped it for both key names and for their claimed literal values ("Dagsspá", "Hámark", "Daily forecast", "High"): **zero matches, in either direction, anywhere in the file.** The EN and IS `weatherVoiceShare*` blocks both end at `weatherVoiceShareImageAlt` — the exact same nine Revision-1 keys I already verified in Round 1, with no Revision-2 additions.

`buildDailyContextLabel` in the current `weatherVoiceShareImage.js` calls `t("weatherVoiceShareDailyForecastLabel")` and `t("weatherVoiceShareHighTempLabel")` unconditionally whenever `t` is a function (it always is in production — `useT.js` is `(key) => translations[lang]?.[key] ?? key`). With the keys absent from the dictionary, `t()` returns the raw key string itself. **Right now, sharing any Tjaldur, in either language, bakes literal text like `weatherVoiceShareDailyForecastLabel 13.09.2026 · Lítil rigning · weatherVoiceShareHighTempLabel 10°C` directly into the exported/downloaded/natively-shared PNG** — this is not a hypothetical edge case, it is the default path for every share, and it directly undermines the exact feature this ticket ships.

This is genuinely confusing given the evidence: `10-revision2-rain-exported.png` and `12-revision2-long-site-name-exported.png` (viewed directly, not just checked for existence) both show *correct* Icelandic labels — "Dagsspá 13.09.2026 · Lítil rigning · Hámark 10°C". I read `verify-weather-voice-share-revision2.cjs`, the script that produced them: it's a genuine Playwright run against a real dev server (`page.goto(BASE_URL)`, real `page.route` fixtures, real `download.saveAs`), not a stub — and `results-revision2.json`'s recorded file sizes match the staged PNGs byte-for-byte, so the evidence-capture process itself is legitimate and was not fabricated. The evidence's own mtimes (~10.5 minutes after `translations.common.js`'s mtime) put it chronologically *after* the file was last saved, so the keys should still have been there when I checked.

I can't explain the gap from here — I only have file-staging tools into this repo, not shell/git access on the Windows machine — but the practical fact that matters for this review is unambiguous: **the file currently on disk, which is what would actually ship, does not contain the keys the report says it does.** Whether that's a lost edit in a multi-file write, a subsequent accidental revert, or something else, someone with shell/git access needs to look at it — and the fix needs to re-verify the *saved file's actual bytes* post-fix, not just trust a screenshot from an earlier run.

### Required for the next revision

1. Add both keys, in both language blocks, to `translations.common.js` — the real Icelandic/English strings already exist as proof-of-correct-output in the Revision 2 evidence screenshots ("Dagsspá" / "Hámark" for IS; presumably "Daily forecast" / "High" for EN per the hardcoded fallback text), so this should be quick to restore.
2. Re-run (or re-verify) the real-browser evidence capture, and this time also grep/read the actual committed `translations.common.js` in the same pass — don't rely on a screenshot alone to confirm an i18n key exists.
3. Two small, non-blocking stale-comment nits found this round, both from the same "policy narrative didn't get updated with the universal-sharing override" pattern — worth a quick pass, not worth blocking on:
   - `WeatherVoiceCard.jsx` (~line 186-189): "shown only when the hook's own conservative editorial policy produced a snapshot" is no longer accurate post-universal-sharing.
   - `translations.common.js` (~line 480-482): "Shown only for the conservative good/excellent editorial promotion policy (weatherVoiceSharePolicy.js)" — same issue, and now sitting directly above the very keys this review is about.

Everything else in Revision 2 — the frozen-snapshot fix, both dialog guards, the renderer geometry fix, the deterministic date formatter, the policy reduction, and the test coverage across all six files — is independently confirmed correct. This REVISE is scoped to the missing i18n keys (and, at Ripley's discretion, the two stale comments); nothing else needs to be redone.
## Ripley — Final assessment, Round 2 correction (2026-09-13)

**Verdict: PASS for the implemented correction; BLOCKED for full ticket completion pending external verification.**

Jonesy's blocking finding concerned two missing daily-context translation keys. I independently checked the current `src/i18n/translations.common.js`: both keys are present in EN and IS with the expected values, matching the renderer lookups. The discrepancy is resolved in the current worktree. I could not rerun targeted tests because execution approval hit the account usage limit; Jonesy's Revision 2 test and browser evidence remain the available validation record.

The implementation otherwise satisfies approved v2: every displayed condition exposes sharing, structural and unsupported-language guards remain, snapshots are frozen, renderer bounds and localized daily context are addressed, and dialog/pilot requirements are documented. No implementation edits were made here.

The workflow remains **BLOCKED** until deployment, real native OS share-sheet verification, and the separately controlled Facebook pilot are completed or explicitly waived by the owner. No commit, push, deployment, publication or issue closure was performed.

## Owner confirmation and Ripley closure (2026-09-15)

**Verdict: PASS; workflow CLOSED by explicit owner acceptance.**

The owner reported: "Já ég prófaði þetta og gat share-að á facebook" (tested the feature and successfully shared to Facebook). Asked explicitly whether Ripley should close #410 using that test as sufficient final verification without further Facebook measurement, the owner replied: "Gerðu það".

This accepts the owner's successful sharing test as the final completion evidence and waives the remaining external-verification closure gates. It does not independently establish the deployment environment, device, native-share versus download path, GA4 verification, or measured Facebook pilot results; none of those details were supplied. No reach, engagement, or campaign outcome is claimed.

The existing implementation PASS is retained; no code changes or additional tests were needed for this documentation-only closure. CURRENT.md is set to CLOSED. #409's separately recorded outstanding validation is unaffected. This closes the local workflow only; no GitHub issue closure, commit, push, deployment, or publication was performed by this session.
